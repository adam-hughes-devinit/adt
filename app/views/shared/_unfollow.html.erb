<%= form_for(current_user.relationships.find_by_followed_id_and_followed_type(object.id, object.class.name),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>