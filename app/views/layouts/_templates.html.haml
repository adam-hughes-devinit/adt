
%script#typeahead_template{type: "text/template"}
	.row-fluid{"data-target" => "<%= target %>".html_safe}

		%p
			<%= english %>
			| 
			%span.muted
				<% if (type=='Content') { %>
				Web Page
				<% } else { %>
				<%= type %> #<%= value %>
				<% } %>
		