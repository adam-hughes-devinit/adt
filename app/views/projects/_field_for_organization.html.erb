<%= f.association :organization,
	# Org types loaded by AJAX on the project form
	input_html: {class: "organization-select"},
	label: false,
	prompt: "Organization Name",
	wrapper_html: {class: wrapper_class } 
%>
