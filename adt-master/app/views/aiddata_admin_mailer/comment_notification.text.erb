NEW COMMENT:
============================

"<%= @comment.content %>"
was posted by <%= @comment.name %> (<%= @comment.email %>) 
<%= time_ago_in_words @comment.created_at %> ago.

See the comment and the project:
<%= project_url @comment.project,
host: Rails.env.production? ? "china.aiddata.org" : "localhost:3000" %>


You receied this email because you are an AidData admin at 
<%= Rails.env.production? ? "China.Aiddata.Org" : "localhost:3000" %>.
Contact rmosolgo@aiddata.org to unsubscribe.

============================
